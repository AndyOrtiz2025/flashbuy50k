server:
  port: 8082

spring:
  application:
    name: catalog-service

  datasource:
    # Cambiamos 5432 por 5440 para que coincida con el docker-compose
    url: jdbc:postgresql://localhost:5440/catalog_db
    username: flashbuy
    password: flashbuy123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info