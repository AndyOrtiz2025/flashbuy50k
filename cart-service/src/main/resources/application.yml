server:
  port: 8082

spring:
  application:
    name: cart-service

  datasource:
    url: ${CART_DB_URL:jdbc:postgresql://localhost:5434/cart_db}
    username: ${DB_USER:flashbuy}
    password: ${DB_PASS:flashbuy123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
